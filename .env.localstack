# =============================================================================
# aws-sandbox LocalStack Configuration - SINGLE SOURCE OF TRUTH
# =============================================================================
# DO NOT DUPLICATE environment variables elsewhere
# All docker-compose files should use ${VAR:-default} syntax to reference these
#
# Constitutional: ADLC v3.0.0 (KISS/DRY/LEAN)
# =============================================================================

# -----------------------------------------------------------------------------
# Network Configuration (172.29.x.x — avoids collision with adlc-network 172.28.x.x)
# -----------------------------------------------------------------------------
NETWORK_NAME=sandbox-network
NETWORK_SUBNET=172.29.0.0/16
NETWORK_GATEWAY=172.29.0.1

# -----------------------------------------------------------------------------
# LocalStack Configuration
# -----------------------------------------------------------------------------
LOCALSTACK_CONTAINER=sandbox-localstack
LOCALSTACK_VERSION=latest
LOCALSTACK_PORT=4566
LOCALSTACK_IP=172.29.0.2
LOCALSTACK_DEBUG=0
LOCALSTACK_SERVICES=s3,dynamodb,sqs,lambda,iam,sts,ssm,events,ses,secretsmanager,kms,stepfunctions,cloudformation,apigateway,logs,sns
LOCALSTACK_VOLUME=sandbox-localstack-data

# -----------------------------------------------------------------------------
# Sandbox Container Configuration
# -----------------------------------------------------------------------------
SANDBOX_CONTAINER=sandbox-dev
SANDBOX_IP=172.29.0.3
TERRAFORM_IMAGE_TAG=latest

# -----------------------------------------------------------------------------
# CDK Configuration (Parameterized — NO HARDCODING)
# -----------------------------------------------------------------------------
CDK_QUALIFIER=hnb659fds
CDK_ACCOUNT=000000000000

# -----------------------------------------------------------------------------
# AWS Configuration
# -----------------------------------------------------------------------------
AWS_REGION=ap-southeast-2

# -----------------------------------------------------------------------------
# Cache Volumes
# -----------------------------------------------------------------------------
TRIVY_VOLUME=sandbox-trivy-cache
NPM_VOLUME=sandbox-npm-cache
