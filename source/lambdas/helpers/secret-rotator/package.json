{
  "name": "sandbox-secret-rotator",
  "description": "Rotates the JWT secret - Lambda - Sandbox for AWS (S101)",
  "version": "1.1.8",
  "scripts": {
    "build": "tsc --incremental --noEmit",
    "test": "vitest run"
  },
  "type": "module",
  "license": "Apache-2.0"
}
